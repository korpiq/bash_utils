# Hardware burn-in — katonkoti26

CPU stress test and disk fileio benchmark, run once to validate the
new katonkoti26 build (Ryzen 9 5900X, NVMe SSDs) after assembly.
Archived rather than kept in `bin/` since they're one-off validation
runs, not ongoing utilities.
